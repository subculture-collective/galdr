---
description: Show possible dead exports for changed files or whole repo
---
Interpret `$ARGUMENTS` as optional `changed|all [base] [path=<subtree>]`.

Use the `review_dead_exports` tool.

Reply with strongest candidates first.
