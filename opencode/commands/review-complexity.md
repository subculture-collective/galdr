---
description: Show complexity hotspots for changed files or whole repo
---
Interpret `$ARGUMENTS` as optional `changed|all [base] [path=<subtree>]`.

Use the `review_complexity` tool.

Reply with highest-risk hotspots first.
