---
description: Show coverage gaps for changed files or whole repo
---
Interpret `$ARGUMENTS` as optional `changed|all [base]`.

Use the `review_coverage_gaps` tool.

Reply with highest-risk gaps first.
